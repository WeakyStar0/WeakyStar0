<div id="project-overlay" class="overlay hidden">
    <div class="overlay-content">
        <button id="close-overlay" class="close-btn">&times;</button>

        <div class="overlay-body">
            <div class="overlay-image">
                <img id="overlay-img" src="" alt="Project Preview" />
            </div>

            <div class="overlay-text">
                <h2 id="overlay-title">Project Title</h2>
                <div id="overlay-tech" class="project-tech"></div>
                <hr />
                <p id="overlay-desc">Detailed description goes here...</p>

                <div class="overlay-extra">
                    <h3>Key Features</h3>
                    <ul id="overlay-features">
                        <li>Feature A: High performance optimization.</li>
                        <li>Feature B: Custom user interface design.</li>
                        <li>Feature C: Scalable backend integration.</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.7);
        backdrop-filter: blur(8px); /* Blur background */
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 2000;
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.3s ease;
    }

    .overlay.active {
        opacity: 1;
        pointer-events: auto;
    }

    .overlay-content {
        background: #1e1e2e;
        width: 80%;
        height: 80%;
        border-radius: 20px;
        border: 1px solid #333;
        position: relative;
        overflow-y: auto; /* Internal scrolling */
        padding: 3rem;
        box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
    }

    /* Custom scrollbar for overlay */
    .overlay-content::-webkit-scrollbar {
        width: 8px;
    }
    .overlay-content::-webkit-scrollbar-thumb {
        background: #6366f1;
        border-radius: 10px;
    }

    .close-btn {
        position: absolute;
        top: 1.5rem;
        right: 1.5rem;
        background: none;
        border: none;
        color: white;
        font-size: 2.5rem;
        cursor: pointer;
        line-height: 1;
    }

    .overlay-body {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 2rem;
    }

    .overlay-image img {
        width: 100%;
        border-radius: 10px;
        border: 1px solid #333;
    }

    .overlay-text h2 {
        font-size: 2.5rem;
        color: #a5b4fc;
        margin-bottom: 1rem;
    }

    hr {
        border: 0;
        border-top: 1px solid #333;
        margin: 1.5rem 0;
    }

    .overlay-text p {
        color: #ccc;
        line-height: 1.8;
        font-size: 1.1rem;
    }

    .overlay-extra {
        margin-top: 2rem;
    }
    .overlay-extra h3 {
        color: #6366f1;
        margin-bottom: 1rem;
    }
    .overlay-extra ul {
        color: #888;
        padding-left: 1.2rem;
    }
    .overlay-extra li {
        margin-bottom: 0.5rem;
    }

    @media (max-width: 900px) {
        .overlay-body {
            grid-template-columns: 1fr;
        }
        .overlay-content {
            width: 95%;
            height: 95%;
            padding: 1.5rem;
        }
    }

    .hidden {
        display: none;
    }
</style>
